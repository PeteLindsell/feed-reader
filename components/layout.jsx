import React from "react";

import Header from "./Header";

const Layout = React.createClass ({
  render() {
    return (
      <div>
        <Header />
      </div>
    );
  }
});

export default Layout;